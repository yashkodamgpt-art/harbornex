import type { ProcessedOptionsWithSinglePlatformArch, DownloadOptions, Options, ProcessedOptions } from './types.js';
export declare class Packager {
    canCreateSymlinks: boolean | undefined;
    opts: ProcessedOptions;
    tempBase: string;
    useTempDir: boolean;
    constructor(opts: ProcessedOptions);
    ensureTempDir(): Promise<void>;
    testSymlink(comboOpts: ProcessedOptionsWithSinglePlatformArch, zipPath: string): Promise<any>;
    skipHostPlatformSansSymlinkSupport(comboOpts: ProcessedOptionsWithSinglePlatformArch): Promise<void>;
    overwriteAndCreateApp(outDir: string, comboOpts: ProcessedOptionsWithSinglePlatformArch, zipPath: string): Promise<any>;
    extractElectronZip(comboOpts: ProcessedOptionsWithSinglePlatformArch, zipPath: string, buildDir: string): Promise<void>;
    buildDir(platform: ProcessedOptionsWithSinglePlatformArch['platform'], arch: ProcessedOptionsWithSinglePlatformArch['arch']): Promise<string>;
    createApp(comboOpts: ProcessedOptionsWithSinglePlatformArch, zipPath: string): Promise<any>;
    checkOverwrite(comboOpts: ProcessedOptionsWithSinglePlatformArch, zipPath: string): Promise<any>;
    getElectronZipPath(downloadOpts: DownloadOptions): Promise<string>;
    packageForPlatformAndArchWithOpts(comboOpts: ProcessedOptionsWithSinglePlatformArch, downloadOpts: DownloadOptions): Promise<any>;
    packageForPlatformAndArch(downloadOpts: DownloadOptions): Promise<any>;
}
/**
 * Bundles Electron-based application source code with a renamed/customized Electron executable and
 * its supporting files into folders ready for distribution.
 *
 * Briefly, this function:
 * - finds or downloads the correct release of Electron
 * - uses that version of Electron to create a app in `<out>/<appname>-<platform>-<arch>`
 *
 * @example
 *
 * ```javascript
 * import { packager } from '@electron/packager'
 *
 * async function bundleElectronApp(options) {
 *   const appPaths = await packager(options)
 *   console.log(`Electron app bundles created:\n${appPaths.join("\n")}`)
 * }
 * ```
 *
 * @param opts - Options to configure packaging.
 *
 * @returns A Promise containing the paths to the newly created application bundles.
 */
export declare function packager(opts: Options): Promise<string[]>;
